import axios from "axios"

export const removeProject = async (projectId: string, accessToken: string) => {
  // return axios.delete(`${process.env.NEXT_PUBLIC_BACKEND_API_URL}`)
  return console.log('delete')
}